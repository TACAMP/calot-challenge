$('.comment_form').html("<%= j(render "comments/form" , post: @post, comment: @comment ) %>");
$('.comment_index').html("<%= j(render "comments/index" , post: @post ) %>");
$('.comment_count').html("<%= j(render "comments/count" , comment: @post.comments ) %>");